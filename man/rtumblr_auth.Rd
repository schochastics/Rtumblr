% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oauth.R
\name{rtumblr_auth}
\alias{rtumblr_auth}
\title{Authenticate with Tumblr (OAuth 1.0a)}
\usage{
rtumblr_auth(app_credentials = NULL, cache = TRUE, force = FALSE)
}
\arguments{
\item{app_credentials}{a named list with \code{consumer_key} and \code{consumer_secret}. If
\code{NULL}, loaded from the \code{RTUMBLR_TOKEN} environment variable.}

\item{cache}{logical, whether to read/write the access token from the on-disk cache.}

\item{force}{logical, if \code{TRUE} re-run the flow even if a cached token exists.}
}
\value{
invisibly, a list with \code{oauth_token} and \code{oauth_token_secret}.
}
\description{
Performs the interactive three-legged OAuth 1.0a flow: it opens a browser so you
can authorize the app, then exchanges the verifier for a durable access token. The
access token is cached on disk (under \code{\link[tools:R_user_dir]{tools::R_user_dir()}}) so the browser only
opens once; subsequent sessions reuse the cached token.
}
\details{
You normally do not need to call this directly: any function that hits an
OAuth-protected endpoint will trigger it automatically when no cached token is found.
As before, you only need to provide your app's consumer key and secret via the
\code{RTUMBLR_TOKEN} environment variable (\code{"consumer_key;consumer_secret"}).
}
\examples{
\dontrun{
rtumblr_auth()
}
}
